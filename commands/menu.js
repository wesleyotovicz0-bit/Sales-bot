const { SlashCommandBuilder, EmbedBuilder, ActionRowBuilder, ButtonBuilder, ButtonStyle } = require('discord.js');\n\nmodule.exports = {\n  name: 'menu',\n  data: new SlashCommandBuilder()\n    .setName('menu')\n    .setDescription('🛍️ Abrir menu principal da loja'),\n  \n  async execute(interaction) {\n    try {\n      const embed = new EmbedBuilder()\n        .setColor('#667eea')\n        .setTitle('🛍️ BEM-VINDO À LOJA!')\n        .setDescription('Escolha uma opção abaixo para começar')\n        .addFields(\n          { name: '💰 Wallet', value: 'Ver saldo e depositar', inline: true },\n          { name: '🛒 Comprar', value: 'Ver produtos', inline: true },\n          { name: '👥 Afiliados', value: 'Ganhar comissões', inline: true }\n        )\n        .setFooter({ text: '💳 Payments: Wallet, Stripe, PIX' });\n\n      const buttons = new ActionRowBuilder()\n        .addComponents(\n          new ButtonBuilder()\n            .setCustomId('menu_wallet')\n            .setLabel('💰 Wallet')\n            .setStyle(ButtonStyle.Primary),\n          new ButtonBuilder()\n            .setCustomId('menu_shop')\n            .setLabel('🛒 Comprar')\n            .setStyle(ButtonStyle.Success),\n          new ButtonBuilder()\n            .setCustomId('menu_affiliate')\n            .setLabel('👥 Afiliados')\n            .setStyle(ButtonStyle.Secondary)\n        );\n\n      await interaction.reply({\n        embeds: [embed],\n        components: [buttons],\n        ephemeral: false\n      });\n    } catch (error) {\n      console.error(error);\n      await interaction.reply('❌ Erro ao abrir menu');\n    }\n  },\n};"